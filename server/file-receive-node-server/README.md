# Reference

```bash
  npm init 
  npm install express cors dotenv nodemon
  npm install  -D typescript ts-node-dev @types/express @types/cors @babel/core @babel/preset-env tsconfig-paths
  npx tsc --init
  npx babel src --out-dir dist

```
